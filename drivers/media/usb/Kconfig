# SPDX-License-Identifier: GPL-2.0-only
#
# NOTE: The nd_controller driver has moved to:
#   techpack/camera/drivers/nd_controller/
#
# That location integrates correctly with the Xiaomi / Qualcomm camera
# techpack build system (Android.mk, BOARD_VENDOR_KERNEL_MODULES, V4L2
# async subdev registration, pm_runtime, USB suspend/resume).
#
# Do not add CONFIG_VIDEO_ND_CONTROLLER here.  Add it to:
#   arch/arm64/configs/vendor/kalama_GKI.config
#   (or the device-specific fragment for thor/ishtar)
